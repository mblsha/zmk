# .gitlint
[general]
ignore=B6  ; body-first-line-empty

[title-match-regex]
# Allow scopes like (ci), (drivers), etc.
regex=(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\([^)]+\))?: .+

[title-max-length]
line-length=72

[body-max-line-length]
line-length=100